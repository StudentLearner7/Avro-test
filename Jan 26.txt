{
  "query": {
    "bool": {
      "must": [
        {
          "wildcard": {
            "value.bpmnProcessId": "*"
          }
        },
        {
          "match": {
            "value.bpmnElementType": "PROCESS"
          }
        }
      ]
    }
  },
  "aggs": {
    "group_by_key": {
      "terms": {
        "field": "key",
        "size": 10000
      },
      "aggs": {
        "filtered_docs": {
          "scripted_metric": {
            "init_script": "state.docs = []",
            "map_script": """
              String intent = doc['intent.keyword'].value;
              if (intent != null && intent.equals('activated')) {
                Map docData = new HashMap();
                docData.put('key', doc['key'].value);
                docData.put('sequence', doc['sequence'].value);
                docData.put('intent', intent);
                state.docs.add(docData);
              }
            """,
            "combine_script": "return state.docs",
            "reduce_script": """
              Map highest = new HashMap();
              for (state in states) {
                for (doc in state) {
                  if (!highest.containsKey(doc['key']) || highest.get(doc['key'])['sequence'] < doc['sequence']) {
                    highest.put(doc['key'], doc);
                  }
                }
              }
              return highest.values();
            """
          }
        }
      }
    }
  },
  "size": 0
}
