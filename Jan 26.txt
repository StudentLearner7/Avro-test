{
  "size": 0,  // We don't need the main query hits, just the aggregation results
  "aggs": {
    "group_by_key": {
      "terms": {
        "field": "key",
        "size": 10  // Adjust the size as needed
      },
      "aggs": {
        "top_documents": {
          "top_hits": {
            "size": 2  // Assuming you want both documents from the buckets
            // Add more settings for top_hits if needed (sort, _source, etc.)
          }
        },
        "bucket_filter": {
          "bucket_selector": {
            "buckets_path": {
              "docCount": "_count"
            },
            "script": "params.docCount == 2"
          }
        }
      }
    }
  }
}